tool_name: "web_search"
description: "Mock/real web search tool (subject-based)."
target_subject: "cg.v1r3.{project_id}.{channel_id}.cmd.tool.search.call"
after_execution: "suspend"
parameters:
  type: "object"
  properties:
    query:
      type: "string"
  required:
    - "query"
options: {}
