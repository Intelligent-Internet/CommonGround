name: "Associate_Search"
worker_target: "worker_generic"
tags:
  - "associate"
description: "Mock associate profile for offline demos (overrides default)."
allowed_tools: []
allowed_internal_tools:
  - "submit_result"
must_end_with:
  - "submit_result"
llm_config:
  model: "mock-direct"
