name: "Mock_Chat_Assistant"
worker_target: "worker_generic"
tags:
  - "partner"
description: "Mock chat agent for UI action demo."
system_prompt_template: |
  You are a mock chat assistant used for local testing.
  Always call submit_result once and only once.
allowed_tools: []
allowed_internal_tools:
  - "submit_result"
must_end_with:
  - "submit_result"
llm_config:
  model: "mock-direct"
