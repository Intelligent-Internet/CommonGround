name: "Mock_Expert_Profile"
worker_target: "worker_generic"
tags:
  - "expert"
description: "Mock expert profile for offline expert flow demo."
allowed_tools:
  - "emit_marker"
allowed_internal_tools:
  - "submit_result"
must_end_with:
  - "submit_result"
llm_config:
  model: "mock-direct"
