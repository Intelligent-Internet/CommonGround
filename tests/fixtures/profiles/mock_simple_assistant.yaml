name: "Mock_Simple_Assistant"
worker_target: "worker_generic"
tags:
  - "principal"
description: "Mock assistant for local script tests."
system_prompt_template: |
  You are a test assistant.
  Always call submit_result once and only once.
allowed_tools: []
allowed_internal_tools:
  - "submit_result"
must_end_with:
  - "submit_result"
llm_config:
  model: "mock-direct"
