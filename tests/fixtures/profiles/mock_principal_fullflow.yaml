name: "Mock_Principal_FullFlow"
worker_target: "worker_generic"
tags:
  - "principal"
description: "Mock principal profile for offline fullflow demo."
allowed_tools: []
allowed_internal_tools:
  - "submit_result"
must_end_with:
  - "submit_result"
llm_config:
  model: "mock-direct"
